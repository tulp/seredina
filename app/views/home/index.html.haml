- content_for :head do
  = stylesheet_link_tag    'app/home/index'
  = javascript_include_tag 'app/home/index', 'snippets/email_validator'

#yandex_maps

#overlay

#dialog
  = form_for :user, :url => users_path, :html => { :id => 'dialog_form' } do |f|
    = f.label       :email, "Ваш email"
    = f.email_field :email
    = f.submit      'OK'

/ убирать div заголовка при наведении
/ текущее положение
/  убрать из шаблона название категории, если будут у котигоризатора картинки
/ подтверждение

/ var toolBar = new YMaps.ToolBar();
/ toolBar.add(new GeolocatorButton());
/ map.addControl(toolBar);
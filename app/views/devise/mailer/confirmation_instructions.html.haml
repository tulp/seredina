%p Welcome #{@resource.email}!

%p Your password: #{@resource.temporary_password_accessor}

%p You can confirm your account through the link below:

%p= link_to 'PUT into locale', confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)